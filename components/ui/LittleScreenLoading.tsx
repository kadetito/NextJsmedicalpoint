import { Box, CircularProgress, Typography } from "@mui/material";
import React from "react";

export const LittleScreenLoading = () => {
  return (
    <>
      <CircularProgress thickness={2} />
    </>
  );
};
